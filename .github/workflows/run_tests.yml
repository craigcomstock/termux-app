name: Unit tests

on:
  push:
    branches:
      - master
      - android-10
      - gesture
  pull_request:
    branches:
      - master
      - android-10
      - gesture

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
    - name: Clone repository
      uses: actions/checkout@v2
    - name: Execute tests
      run: |
        ./gradlew test
